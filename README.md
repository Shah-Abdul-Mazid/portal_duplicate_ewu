# EWU Portal Clone Development

I have fully rebuilt the East West University login portal interface using React and modern CSS variables.

The aesthetic now perfectly matches the provided screenshot, complete with:
- **Full Screen Dynamic Background**: Created a beautiful university-at-night AI generated image that spans the background.
- **Glassmorphic Right Sidebar**: Built a 450px wide right-aligned blurred panel that the form sits inside.
- **Authentic Form Elements**: Accurately recreated the `Student ID` and `Password` inputs (bottom border only, including a functional show/hide password eye icon). Included the structural elements for the basic math CAPTCHA.
- **Buttons and Typography**: Cloned all form buttons with precise original colors: the teal `SIGN IN`, the blue `Reset Password`, the red `Activate ID` and the light blue `Admin & Faculty Panel` (with an admin icon).
